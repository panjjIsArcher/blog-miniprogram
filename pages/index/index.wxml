<view class="index">
    <codeRain wx:if="{{showCodeRain}}" text="{{codeList}}" bind:animationEnd="animationEnd"/>
    <!-- <pill wx:if="{{showPill}}" /> -->
    <!-- <pill  /> -->
    <!-- <welcome  wx:if="{{showCanvas}}"/> -->
</view>